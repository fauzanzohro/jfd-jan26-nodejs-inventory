<%- include('../layout/menu_atas') %>
<%- include('../layout/side_bar') %>
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
<h1 class="h2">Detail Produk</h1>
</div>
<table class="table table-hover table-bordered text-center">
    <thead>

        <tr>
            <th>Kode</th>
            <th>Nama</th>
            <th>Deskripsi</th>
            <th>Foto</th>
        </tr>
    </thead>
    <tbody>
            <tr>
                <td><%= detail_produk[0].kode %></td>
                <td><%= detail_produk[0].nama %></td>
                <td><%= detail_produk[0].deskripsi %></td>
                <td>
                     <% if (detail_produk[0].foto) { %>
                        <img src="/upload-image/<%= detail_produk[0].foto %>" alt="foto produk <%= detail_produk[0].nama %>" width="100">
                    <% } %>
                </td>
            </tr>
    </tbody>
</table>